# 安全策略

## 报告安全问题

请不要在公开 Issue 中披露可被利用的漏洞、密钥、客户数据或完整攻击步骤。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。如果该入口暂时不可用，请联系仓库维护者，并只提供定位问题所需的最少信息。

报告建议包含：

- 受影响的提交、版本或接口；
- 可复现步骤和预期影响；
- 是否涉及认证绕过、越权、租户数据泄漏、危险 SQL、审批重放或重复副作用；
- 已知的临时缓解措施；
- 不包含真实密钥和客户数据的最小复现样例。

## 安全边界

本项目重点保护以下边界：

- API Key/JWT 认证与角色授权；
- tenant/store 级数据隔离和 PostgreSQL RLS；
- Text-to-SQL 的只读、表字段、函数和复杂度限制；
- 高风险写操作的人工审批、参数绑定、过期和一次性消费；
- Skill 幂等、防重复执行与审计记录；
- RAG 引用、证据来源和 grounding 状态；
- 浏览器凭证仅保存在当前标签页的 `sessionStorage`。

## 敏感信息

仓库中的 `.env.example` 只用于展示配置项。请勿提交真实 API Key、数据库密码、令牌、私有证书、客户订单、聊天记录或生产日志。若敏感信息已经进入 Git 历史，请立即轮换凭证，并在私密安全报告中说明影响范围。

## 支持范围

安全修复以 `main` 分支为准。Demo provider、示例数据和本地开发配置不代表生产部署方案；生产环境仍需使用托管密钥、正式数据库角色、网络隔离、备份与监控策略。

